spring:
  config.activate.on-profile: dev
  jpa.show-sql: false
logging.level.com.dpworld.fms: DEBUG
dpwfms:
  development:
    simulator-enabled: ${SIMULATOR_ENABLED:true}
  cors.allowed-origins: ${CORS_ALLOWED_ORIGINS:http://localhost:5173,http://localhost:3000}
