import{useApi}from'../api/useApi';import{Empty,Failure,Loading,PageHeader,Status}from'../components/ui';type Job={id:string;job_number:string;job_type:string;status:string;priority:number;fleet_number?:string;created_at:string};export default function JobsPage(){const{data,loading,error}=useApi<Job[]>('/api/workspace/jobs');if(loading)return <Loading/>;if(error)return <Failure message={error}/>;return <><PageHeader eyebrow="DISPATCH" title="Jobs and dispatch" description="Persistent parking, charging, fueling and transport work."/><section className="panel table-panel">{data?.length?<table><thead><tr><th>Job</th><th>Type</th><th>Asset</th><th>Priority</th><th>Status</th><th>Created</th></tr></thead><tbody>{data.map(j=><tr key={j.id}><td>{j.job_number}</td><td>{j.job_type}</td><td>{j.fleet_number??'Unassigned'}</td><td>{j.priority}</td><td><Status value={j.status}/></td><td>{new Date(j.created_at).toLocaleString()}</td></tr>)}</tbody></table>:<Empty message="No jobs have been created."/>}</section></>}
