import{useApi}from'../api/useApi';import{Failure,Kpi,Loading,PageHeader,Status}from'../components/ui';type Overview={totalFleet:number;availableVehicles:number;reservedVehicles:number;assignedVehicles:number;fleetInOperation:number;idleVehicles:number;parkedVehicles:number;chargingVehicles:number;waitingForCharging:number;fuelingVehicles:number;maintenanceVehicles:number;offlineVehicles:number;staleTelemetry:number;activeJobs:number;failedJobs:number;parkingOccupied:number;parkingCapacity:number;chargingOccupied:number;chargingCapacity:number;criticalAlerts:number;lastTelemetryAt?:string;fuelStations:number;chargingStations:number};const cards:(keyof Overview|string)[][]=[['totalFleet','Total assets','▰','cyan','vehicles'],['availableVehicles','Available','A','green','vehicles'],['reservedVehicles','Reserved','R','blue','vehicles'],['assignedVehicles','Assigned','→','blue','jobs'],['fleetInOperation','Moving / working','⇥','green','map'],['idleVehicles','Idle','I','orange','vehicles'],['parkedVehicles','Parked','P','orange','parking'],['chargingVehicles','Charging','ϟ','blue','charging'],['waitingForCharging','Waiting to charge','Q','orange','charging'],['fuelingVehicles','Fueling','F','blue','vehicles'],['maintenanceVehicles','Maintenance','M','red','vehicles'],['offlineVehicles','Offline','○','grey','map'],['staleTelemetry','Stale telemetry','!','orange','map'],['activeJobs','Active jobs','J','blue','jobs'],['failedJobs','Failed jobs','×','red','jobs'],['criticalAlerts','Critical alerts','!','red','alerts']];export default function OverviewPage(){const{data,loading,error}=useApi<Overview>('/api/workspace/overview');if(loading)return <Loading/>;if(error||!data)return <Failure message={error??'Dashboard unavailable'}/>;return <><PageHeader eyebrow="TERMINAL CONTROL ROOM" title="Operations dashboard" description={`Real operational state · last telemetry ${data.lastTelemetryAt?new Date(data.lastTelemetryAt).toLocaleString():'never'}`} actions={<a className="button primary" href="#/map">Open live map →</a>}/><section className="kpi-grid operations-kpis">{cards.map(([key,label,icon,tone,route])=><a className="kpi-link" href={`#/${route}`} key={key}><Kpi label={label} value={data[key as keyof Overview] as number} icon={icon} tone={tone}/></a>)}</section><div className="dashboard-grid"><section className="panel"><h2>Parking occupancy</h2><progress value={data.parkingOccupied} max={Math.max(1,data.parkingCapacity)}/><p>{data.parkingOccupied} occupied of {data.parkingCapacity} bays</p><a href="#/parking">Open parking operations →</a></section><section className="panel"><h2>Charging utilization</h2><progress value={data.chargingOccupied} max={Math.max(1,data.chargingCapacity)}/><p>{data.chargingOccupied} active of {data.chargingCapacity} simultaneous positions</p><a href="#/charging">Open charging operations →</a></section></div><section className="panel activity"><div><i>↯</i><span><small>INTEGRATION HEALTH</small><b>Health is reported as UNKNOWN unless verified by a real adapter check.</b></span></div><Status value="UNKNOWN"/></section></>}
