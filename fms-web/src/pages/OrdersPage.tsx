import { useApi } from '../api/useApi';import { Empty,Failure,Loading,PageHeader,Status } from '../components/ui';
type Order={id:string;order_number:string;plant:string;order_type:string;priority:number;status:string;created_at:string;completed_at?:string};
export default function OrdersPage(){const{data,loading,error}=useApi<Order[]>('/api/workspace/orders');if(loading)return <Loading/>;if(error)return <Failure message={error}/>;return <><PageHeader eyebrow="EXECUTION" title="Transport orders" description="Create, prioritize, assign, retry and trace terminal movements." actions={<button className="primary">+ Create order</button>}/><div className="toolbar"><input placeholder="Search orders…"/><select><option>All states</option></select><select><option>All plants</option></select></div><section className="panel table-panel">{data?.length?<table><thead><tr><th>Order</th><th>State</th><th>Plant</th><th>Type</th><th>Priority</th><th>Created</th><th>Finished</th><th/></tr></thead><tbody>{data.map(o=><tr key={o.id}><td><b>{o.order_number}</b></td><td><Status value={o.status}/></td><td>{o.plant}</td><td>{o.order_type}</td><td>{o.priority}</td><td>{new Date(o.created_at).toLocaleString()}</td><td>{o.completed_at?new Date(o.completed_at).toLocaleString():'—'}</td><td><button>View</button></td></tr>)}</tbody></table>:<Empty message="No transport orders have been accepted."/>}</section></>}
