import { PageHeader } from '../components/ui';
const cards=[['Job queue','Validated, waiting and assigned operational work'],['Scheduling rules','Capability, priority, energy and plant constraints'],['Dispatch history','Commands, acknowledgements, retries and overrides'],['Alerts','Safety, operations and integration exceptions'],['Reports','Utilization, movement, energy and audit exports']];
export default function OperationsPage(){const title=location.hash.split('/')[1]?.replace('-',' ')||'Operations';return <><PageHeader eyebrow="OPERATIONS" title={title} description="Permission-controlled operational workspace backed by DPW FMS services."/><section className="module-grid">{cards.map(([name,description])=><article className="panel" key={name}><i>◇</i><h2>{name}</h2><p>{description}</p><button>Open workspace →</button></article>)}</section></>}
