import{useApi}from'../api/useApi';import{Empty,Failure,Loading,PageHeader}from'../components/ui';type Audit={id:string;occurred_at:string;actor:string;action:string;resource_type:string;resource_id:string;correlation_id?:string};export default function AuditPage(){const{data,loading,error}=useApi<Audit[]>('/api/workspace/audit');if(loading)return <Loading/>;if(error)return <Failure message={error}/>;return <><PageHeader eyebrow="GOVERNANCE" title="Assignment and configuration audit" description="Immutable operator, automation and override history without secrets."/><section className="panel table-panel">{data?.length?<table><thead><tr><th>Time</th><th>Actor</th><th>Action</th><th>Resource</th><th>Correlation</th></tr></thead><tbody>{data.map(a=><tr key={a.id}><td>{new Date(a.occurred_at).toLocaleString()}</td><td>{a.actor}</td><td>{a.action}</td><td>{a.resource_type} / {a.resource_id}</td><td>{a.correlation_id??'—'}</td></tr>)}</tbody></table>:<Empty message="No audited changes."/>}</section></>}
