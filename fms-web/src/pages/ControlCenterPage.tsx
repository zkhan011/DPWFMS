import { useApi } from '../api/useApi';import { Failure,Kpi,Loading,PageHeader,Status } from '../components/ui';
type Health={backend:string;database:string;kernel:Record<string,unknown>;trackIt:Record<string,unknown>;mqtt:Record<string,unknown>;rabbitMq:Record<string,unknown>;checkedAt:string};
export default function ControlCenterPage(){const{data,loading,error}=useApi<Health>('/api/workspace/control-center');if(loading)return <Loading/>;if(error||!data)return <Failure message={error??'Health unavailable'}/>;const state=(x:Record<string,unknown>)=>String(x.health_status??(x.enabled?'UNKNOWN':'DISABLED'));return <><PageHeader eyebrow="SYSTEM OPERATIONS" title="Control center" description="Integration health, background processes and recovery actions."/><section className="kpi-grid compact"><Kpi label="Backend" value={<Status value={data.backend}/>} icon="✓" tone="green"/><Kpi label="Database" value={<Status value={data.database}/>} icon="DB" tone="cyan"/><Kpi label="Kernel" value={<Status value={state(data.kernel)}/>} icon="K"/><Kpi label="MQTT" value={<Status value={state(data.mqtt)}/>} icon="M" tone="purple"/></section><section className="panel table-panel"><table><thead><tr><th>Integration</th><th>Status</th><th>Last communication</th><th>Error</th><th/></tr></thead><tbody>{[['Kernel',data.kernel],['TrackIT',data.trackIt],['MQTT',data.mqtt],['RabbitMQ',data.rabbitMq]].map(([name,value])=>{const v=value as Record<string,unknown>;return <tr key={name as string}><td><b>{name as string}</b></td><td><Status value={state(v)}/></td><td>{String(v.last_success_at??'Never')}</td><td>{String(v.last_error??'—')}</td><td><button>Test</button></td></tr>})}</tbody></table></section><div className="warning panel"><b>Sensitive operations</b><p>Start, stop and retry operations require explicit confirmation, a reason and control_center.operate permission.</p></div></>}
